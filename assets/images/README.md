# Couple profile images

Replace these placeholder files with your own photos while keeping the same paths:

- `assets/images/anu/profile.svg` for Anu
- `assets/images/varun/profile.svg` for Varun

PNG, JPG, WEBP, or SVG files all work. If you change the filenames, update the matching `<img>` paths in `index.html`.
